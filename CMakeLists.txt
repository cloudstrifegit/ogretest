CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES($ENV{OGRE_HOME}/include)
LINK_DIRECTORIES($ENV{OGRE_HOME}/lib)
LINK_DIRECTORIES($ENV{OGRE_HOME}/lib/opt)

PROJECT(ogretest)

ADD_SUBDIRECTORY(src)